name: Deploy Pages

on:
  push:
    branches:
      - master
      - "3.15"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: ["3.15"]
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: 3.12
      - uses: actions/checkout@v6
        with:
          repository: python/cpython
          ref: ${{ matrix.version }}
      - run: make venv
        working-directory: ./Doc
      - uses: actions/checkout@v6
        with:
          ref: ${{ matrix.version }}
          path: Doc/locales/vi/LC_MESSAGES
      - uses: sphinx-doc/github-problem-matcher@v1.1
      - run: make -e SPHINXERRORHANDLING="--keep-going" SPHINXOPTS="--color -D language='vi'" html
        working-directory: ./Doc
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: Doc/build/html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
